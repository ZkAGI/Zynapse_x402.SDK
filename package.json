{
  "name": "zynapse-sdk",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint || true",
    "test": "pnpm -r run test || true"
  }
}

